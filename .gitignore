node_modules/
dist/
*.js.map
.env
.env.local
.env.*.local
AUDIT_REPORT.md
coverage/
*.tsbuildinfo

# generated stack output (test artifact)
.vibe/STACK.md
.vibe/CONTEXT.md
.vibe/report/

# vibe-harness — keep spec files versioned
!.vibe/

# runtime caches — regenerated by `audit`, never version them
.vibe/.audit-cache.json

# mkdocs build output (generated, uploaded by docs.yml)
docs-build/

# pages staging (assembled only in CI)
pages-staging/
