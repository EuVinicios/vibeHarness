---
description: Deprecated guided flow — prefer /status or the vibe_* MCP tools
---

`start` is deprecated since v0.7.0. Use `npx @vibeharness/cli status` (or the MCP tool `vibe_status`) instead — it shows the same lifecycle state non-interactively and hands a ready prompt to the AI.
